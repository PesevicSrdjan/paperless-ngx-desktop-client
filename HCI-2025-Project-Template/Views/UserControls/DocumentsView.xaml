<UserControl x:Class="HCI_2025_Project_Template.Views.UserControls.DocumentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:HCI_2025_Project_Template.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="LoadingCircularProgressBar" TargetType="ProgressBar" BasedOn="{StaticResource MaterialDesignCircularProgressBar}">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="IsIndeterminate" Value="True"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Setter Property="Margin" Value="20,0,0,0"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>

        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/> 
        </Grid.ColumnDefinitions>

        <TextBlock 
           Text="Documents" 
           FontSize="30"
           HorizontalAlignment="Left"
           Margin="20,20,0,0"
           FontWeight="SemiBold"/>

        <StackPanel
            Grid.Row="0"
            Grid.Column="1"
            Orientation="Horizontal" 
            HorizontalAlignment="Right" 
            Margin="0,20,20,0">

            <Button 
                Style="{StaticResource MaterialDesignToolButton}"
                ToolTip="Card View"
                Margin="0,0,10,0"
                Click="cardView_Click">
                
                <materialDesign:PackIcon 
                    Kind="ViewModule" 
                    Width="24" 
                    Height="24"
                    Foreground="#17541F"/>
                
            </Button>

            <Button 
                Style="{StaticResource MaterialDesignToolButton}"
                ToolTip="Table View"
                Click="tableView_Click">
                
                <materialDesign:PackIcon 
                    Kind="ViewList" 
                    Width="24" 
                    Height="24"
                    Foreground="#17541F"/>
                
            </Button>
        </StackPanel>

        <TextBox
            Grid.Row="1"
            Grid.ColumnSpan="2"
            x:Name="textBox_search"
            Height="26"
            VerticalAlignment="Top"
            HorizontalAlignment="Stretch"
            Margin="20,12,20,0"
            Background="White"
            Padding="5,2,5,2"
            Foreground="Black"
            FontSize="15"
            BorderBrush="#666"
            materialDesign:HintAssist.Hint="Search"
            materialDesign:TextFieldAssist.HasLeadingIcon="True"
            materialDesign:TextFieldAssist.HasClearButton="True"
            materialDesign:TextFieldAssist.LeadingIcon="Search"
            Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

        <!--Ostavljen prostor za filtere-->
        <TextBlock
            Text="Filters"
            Height="30"
            Grid.Row="2"
            Grid.ColumnSpan="2"
            HorizontalAlignment="Center"/>

        <!--Ostavljen prostor za broj dokumenata i za Paging-->
        <TextBlock
            Text="Number of doc and Paging"
            Height="30"
            Grid.Row="3"
            Grid.ColumnSpan="2"
            HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="3" Grid.Column="0" HorizontalAlignment="Left" Orientation="Horizontal">

            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <!-- ProgressBar za učitavanje -->
                <ProgressBar 
                    x:Name="loadingBar"
                    Style="{StaticResource LoadingCircularProgressBar}">
                </ProgressBar>

                <TextBlock
                    Text="Loading..."
                    FontStyle="Italic"
                    Foreground="Gray"
                    Margin="10,5,0,0">
                    
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>

                <!-- TextBlock sa brojem dokumenata -->
            <TextBlock 
                FontWeight="SemiBold"
                FontSize="14"
                Margin="20,0,0,0"
                VerticalAlignment="Center"
                Text="{Binding TotalDocuments, StringFormat='Documents: {0}'}">
               
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
                
            </TextBlock>
        </StackPanel>

        <StackPanel 
            Orientation="Horizontal" 
            HorizontalAlignment="Right"
            Grid.Row="3" 
            Grid.Column="1">

            <Button 
                Style="{StaticResource MaterialDesignFlatButton}" 
                Padding="2" Margin="0,0,0,0"
                Width="28" Height="28"
                Click="PreviousPage_Click">

                <materialDesign:PackIcon 
                    Kind="ChevronDoubleLeft" 
                    Width="25" 
                    Height="25"/>
            </Button>

            <Button 
                Style="{StaticResource MaterialDesignFlatDarkBgButton}" 
                Padding="0"
                Width="30" 
                Height="30"
                Foreground="Black"
                Background="White">

                <TextBlock Text="{Binding CurrentPage}"
                    Foreground="Black"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"/>
            </Button>

            <Button 
                Style="{StaticResource MaterialDesignFlatButton}"
                Padding="2"
                Margin="0,0,20,0"
                Width="28" Height="28"
                Click="NextPage_Click">

                <materialDesign:PackIcon 
                    Kind="ChevronDoubleRight" 
                    Width="25" 
                    Height="25"/>

            </Button>
        </StackPanel>
        <ContentControl x:Name="DocumentContentControl" Grid.Row="4" Grid.ColumnSpan="2"></ContentControl>
    </Grid>
</UserControl>
