<UserControl x:Class="HCI_2025_Project_Template.Views.UserControls.TagsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HCI_2025_Project_Template.Views.UserControls"
             mc:Ignorable="d"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>

        <!-- ========== COLORS ========== -->
        <SolidColorBrush x:Key="LoadingForeground">Gray</SolidColorBrush>

        <!-- ========== CONTROL SIZES ========== -->
        <sys:Double x:Key="ProgressBarWidth">20</sys:Double>
        <sys:Double x:Key="ProgressBarHeight">20</sys:Double>
        <sys:Double x:Key="CreateIconWidth">16</sys:Double>
        <sys:Double x:Key="CreateIconHeight">19</sys:Double>
        <sys:Double x:Key="CreateButtonWidth">80</sys:Double>

        <!-- ========== MARGINS ========== -->
        <Thickness x:Key="ProgressBarMargins">20,0,0,0</Thickness>
        <Thickness x:Key="TitleMargins">20,20,0,0</Thickness>
        <Thickness x:Key="CreateIconMargins">0,0,4,0</Thickness>
        <Thickness x:Key="CreateButtonMargins">0,20,30,0</Thickness>
        <Thickness x:Key="LoadingTextMargins">10,10,0,0</Thickness>

        <!-- ========== PADDING ========== -->
        <Thickness x:Key="CreateButtonPadding">5,2</Thickness>

        <Style x:Key="LoadingCircularProgressBar" TargetType="ProgressBar" BasedOn="{StaticResource MaterialDesignCircularProgressBar}">
            <Setter Property="Width" Value="{StaticResource ProgressBarWidth}"/>
            <Setter Property="Height" Value="{StaticResource ProgressBarHeight}"/>
            <Setter Property="IsIndeterminate" Value="True"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Setter Property="Margin" Value="{StaticResource ProgressBarMargins}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Grid 
        Background="{DynamicResource MaterialDesignPaper}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0"
            Style="{StaticResource BodyTextStyle}"
            Text="Tags"
            FontSize="{StaticResource FontSizeTitle}"
            FontWeight="SemiBold"
            Margin="{StaticResource TitleMargins}"/>

        <Button Grid.Row="0"
            Style="{StaticResource MaterialDesignOutlinedButton}"
            BorderBrush="{DynamicResource AccentColor}"
            Foreground="{DynamicResource AccentColor}"
            Width="{StaticResource CreateButtonWidth}"
            HorizontalAlignment="Right"
            Margin="{StaticResource CreateButtonMargins}"
            Padding="{StaticResource CreateButtonPadding}"
            Click="CreateTag_Click">
            
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <materialDesign:PackIcon 
                    Kind="Plus" 
                    Width="{StaticResource CreateIconWidth}" 
                    Height="{StaticResource CreateIconHeight}" 
                    Margin="{StaticResource CreateIconMargins}"/>
                <TextBlock 
                    Text="Create"/>
            </StackPanel>
        </Button>

        <StackPanel Grid.Row="1" HorizontalAlignment="Left" Orientation="Horizontal">
            <!-- ProgressBar za učitavanje -->
            <ProgressBar 
                x:Name="loadingBar"
                Style="{StaticResource LoadingCircularProgressBar}">    
            </ProgressBar>

            <TextBlock
                Text="Loading..."
                FontStyle="Italic"
                Foreground="{StaticResource LoadingForeground}"
                Margin="{StaticResource LoadingTextMargins}"> 

                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

        <ContentControl x:Name="TagsContentControl" Grid.Row="2"></ContentControl>
    </Grid>
</UserControl>
